#include <stdio.h>
int main ()
{
	int a, b, i=1, nod;
	scanf ("%d %d", &a, &b);
	while (i<=a && i<=b) {
		if (a%i==0 && b%i==0) {
			nod=i;
			i++;
		}
		else {i++;}
	}
    printf("%d\n", nod);
	return 0;
}
