#include <stdio.h>
#include <string.h>
int main ()
{
char str[40];
int i=0;
char letter, a;
fgets(str, 40, stdin);
int n;
n = strlen(str);
a=str[i];
while (i+1<n) {
	if (str[i+1]>str[i]) {
		a=str[i+1];
        i++;
	}
	else {
		i++;
	}
}
printf("%d\n", a);
return 0;
}
