#include <stdio.h>
#include <math.h>

int main ()
{
int a, b, c, D;
float x1, x2;
scanf ("%d %d %d", &a, &b, &c);
D=b*b-4*a*c;
if (D<0) {
	printf("нет корней\n");
}
else {
	x1=(sqrt(D)-b)/(2*a);
	x2=((-1)*b-sqrt(D))/(2*a);
}
printf("%.2f %.2f\n", x1, x2);
return 0;
}
