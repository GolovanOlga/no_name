#include <stdio.h>
#include <string.h>
int main ()
{
char str[40];
int i=0;
char letter;
fgets(str, 40, stdin);
int n;
n = strlen(str);
while (i<n) {
	if (str[i]!=32) {
		letter = str[i]-32;
		printf("%c", letter);
        i++;
	}
	else {
		printf(" ");
		i++;
	}
}
printf("\n");
return 0;
}
